i18n.merge_file(
    input: 'schedules.metainfo.xml.in',
    output: gettext_name + '.metainfo.xml',
    po_dir: meson.source_root() / 'po' / 'extra',
    type: 'xml',
    install: true,
    install_dir: datadir / 'metainfo'
)
